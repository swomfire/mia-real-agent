import styled from 'styled-components';

export const CreditTimeWrapper = styled.div`
  display: flex;
  align-items: center;
  margin-bottom: 1em;
  padding: 0 .5em;
  span {
    font-size: 1.1em;
    font-weight: 600;
  }
  button {
    margin-left: 1em;
    position: absolute;
    right: .5em;
    font-size: 1em;
  }
`;
